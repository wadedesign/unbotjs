module.exports = {
    name: "unbotjs",
    script: "src/index.js",
    interpreter: "~/.bun/bin/bun",
  };
  

// how to run bun and pm2 together

// bun guide https://bun.sh/guides/ecosystem/pm2

// make sure bun is in the home directory // the interpreter is the path to the bun executable

// if gives error prob rename back to pm2.config.js